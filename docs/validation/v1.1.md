# v1.1 validation record

Validation date: 2026-08-04. Target version: 1.1.0.

## Completed host validation

| Check | Result |
|---|---|
| Go formatting, unit tests, Linux race tests, and vet | PASS |
| Backend/config/cache/brightness/power/battery tests | PASS |
| Device API auth/ID/version/battery-voltage headers | PASS |
| Redirect credential isolation and HTTPS image policy | PASS |
| Conditional image revalidation and `Retry-After` parsing | PASS |
| Invalid/oversized image rejection | PASS |
| Installer payload/path/template tests | PASS |
| Qt QML lint, resource compile, ARM64 cross-build, bundle self-check | PASS |
| AppLoad SOCK_SEQPACKET + mock Device API integration | PASS |
| ShellCheck 0.11.0 | PASS |
| `govulncheck` 1.6.0 reachable vulnerabilities | PASS (0) |
| Windows VersionInfo/icon/manifest generation | PASS |
| Release ZIP, CycloneDX SBOM, and two-build reproducibility | PASS |

The vulnerability scan reported no reachable vulnerabilities. One advisory in
a required module was not reachable from imported/called code; release review
should still reassess it when dependencies change.

The built Windows installer reports product/file version `1.1.0.0`, product
name `TRMNL for reMarkable`, original filename `TRMNL Installer.exe`, and the
documented application description/company/copyright/comments. Local validation
reports `NotSigned`; Authenticode requires the maintainer's external certificate.

## CI/release validation added

CI now includes race tests on Ubuntu, vet, ShellCheck, Python compile, QML lint,
Qt resource compilation, ARM64 build, the AppLoad protocol harness,
`govulncheck`, and CodeQL with corrected read permissions. The tag workflow runs
the same release blockers before Windows packaging, supports Authenticode secrets,
and attests public artifacts.

## Not yet complete: release blockers requiring external state/hardware

- Exact final v1.1 ZIP installed and exercised on a physical Paper Pro.
- Real hosted-account test with the v1.1 Device API key and active color plugin.
- v1.1 firmware matrix on both 3.26.x and 3.27.x.
- Long battery-runtime result (the test feature is implemented; duration evidence
  cannot be manufactured in a host run).
- Authenticode signature from a publicly trusted certificate.
- Green GitHub CI/CodeQL run on the final pushed commit and public attestation.

Prior physical evidence in `ACCEPTANCE_CHECKLIST.md` came from the 1.0-era app on
Paper Pro firmware 3.27.3.0. It is useful regression context but is not a
substitute for the exact-archive v1.1 physical gate.
